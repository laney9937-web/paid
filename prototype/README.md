# Paid Prototype

Static, non-production UX prototype only. It processes no real payments and stores no user data.

Preview locally with any static HTTP server, for example `python -m http.server 8080` from this folder.

Use `?mode=buyer` to open the buyer transaction view directly.
